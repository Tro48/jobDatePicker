/**
 * Что изменилось в выпусках — список для шторки «Что нового».
 *
 * Записи опознаются датой, а не версией приложения: версию поднимает CI уже
 * после мержа, и в момент, когда пишется эта строка, её ещё не существует.
 *
 * Ни React, ни React Native здесь нет намеренно: тот же список читает сборка,
 * чтобы положить заголовок последней записи в список выпусков — телефону со
 * старым JS взять текст про новую сборку больше неоткуда.
 *
 * Порядок — от свежего к старому, это проверяется тестом.
 */
export interface ReleaseNote {
  /** Дата выпуска в формате YYYY-MM-DD. Она же идентификатор записи. */
  id: string;
  /** Одна строка о главном: она уходит в список выпусков и в полоску календаря. */
  title: string;
  /** Подробности, по пункту на изменение. */
  items: string[];
}

export const RELEASE_NOTES: ReleaseNote[] = [
  {
    id: '2026-09-05',
    title: 'Свои смены, свой график и обмен без облака',
    items: [
      'Смены теперь свои: вечернюю, подработку или учёбу можно завести самому — «Настройки → График → Смены». У встроенных смен правится время, перерыв, надбавка и цвет.',
      'График можно собрать руками: цикл любой длины или раскладка по дням недели. Кнопка «Собрать свой» в выборе графика.',
      'Резервная копия: «Настройки → Данные → Сохранить копию». Файл кладётся куда угодно и возвращает всё обратно на новом телефоне.',
      'График передаётся другому телефону QR-кодом с экрана или файлом в мессенджер — без облака и без учётной записи. Заметки и выплаты по умолчанию не уезжают.',
      'Месяц выгружается в PDF: сетка, часы по дням, отклонение от графика и суммы — то, по чему сверяют табель. Прошлые месяцы тоже.',
      'Виджет на главном экране: четыре недели от текущей, сегодняшний день выделен, нажатие открывает приложение.',
      'Праздники: российский производственный календарь помечает их значком в клетке, а графику по дням недели делает день нерабочим. Выключается в настройках.',
      'Экран «Будильник не звонит»: что включить в Xiaomi, Huawei, Oppo, Vivo и Samsung, чтобы оболочка не выгружала приложение вместе с расписанием.',
      'История графиков: перевели с пятидневки на 2/2 или сменил работу — добавь новый период в «Изменить график», и прожитые месяцы останутся посчитанными по прежнему графику.',
      'Часы и смены считаются с даты первой смены: месяцы до устройства на работу больше не выдают полную норму. Дни до неё в календаре показаны бледно — график их рисует, но в счёт они не идут.',
      'Сетка календаря больше не режет цифры при крупном системном шрифте.',
    ],
  },
  {
    id: '2026-09-03',
    title: 'Видно, сколько уже отработано',
    items: [
      'Сводка и строка под календарём показывают смены и часы дробью: сколько отработано из запланированного на месяц.',
      'Отработанные смены в календаре приглушены.',
      'День, где часы разошлись с графиком, помечен точкой: зелёной при переработке, красной при недоработке. Под календарём — итог за месяц.',
      'Смена и тип выплаты в карточке дня выбираются выпадающим списком — карточка стала короче.',
    ],
  },
];

/** Самая свежая запись. null — списка ещё нет. */
export const LATEST_RELEASE_ID: string | null = RELEASE_NOTES[0]?.id ?? null;

/**
 * Записи, которых человек ещё не видел.
 *
 * null вместо отметки — это обновление с прошлой схемы хранилища: показываем
 * всё, что накопилось. У новой установки отметка сразу стоит на свежей записи,
 * и «что нового» ей не показывается: человеку, который только поставил
 * приложение, нечего рассказывать про изменения.
 */
export function unseenReleases(lastSeenId: string | null): ReleaseNote[] {
  if (lastSeenId === null) return RELEASE_NOTES;
  return RELEASE_NOTES.filter((note) => note.id > lastSeenId);
}
