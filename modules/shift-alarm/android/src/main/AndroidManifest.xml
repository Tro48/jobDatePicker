<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <!-- Точные будильники: SCHEDULE_EXACT_ALARM для Android 12–13, USE_EXACT_ALARM
       для 14+, где приложениям-будильникам оно выдаётся без запроса. -->
  <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
  <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
  <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />
  <uses-permission android:name="android.permission.VIBRATE" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

  <application>
    <!-- Имена классов полные: в манифесте библиотеки короткая запись
         разворачивается по пакету приложения, а не модуля. -->
    <receiver
      android:name="com.andrey.jobdatepicker.alarm.AlarmReceiver"
      android:exported="false" />

    <receiver
      android:name="com.andrey.jobdatepicker.alarm.BootReceiver"
      android:enabled="true"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.TIME_SET" />
        <action android:name="android.intent.action.TIMEZONE_CHANGED" />
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
      </intent-filter>
    </receiver>

    <service
      android:name="com.andrey.jobdatepicker.alarm.AlarmService"
      android:exported="false"
      android:foregroundServiceType="mediaPlayback" />

    <activity
      android:name="com.andrey.jobdatepicker.alarm.AlarmActivity"
      android:exported="false"
      android:excludeFromRecents="true"
      android:launchMode="singleInstance"
      android:showWhenLocked="true"
      android:turnScreenOn="true"
      android:taskAffinity=""
      android:theme="@android:style/Theme.DeviceDefault.NoActionBar" />
  </application>
</manifest>
